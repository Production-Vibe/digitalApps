# Отчёт. Этап «Сборка стека OpenCode» (Stage 0–4)

**Дата:** 02.09.2026
**Статус:** Завершён (код-модули не трогались, деплой не требуется).

## Что сделано

- Создан `AGENTS.md` — контракт работы над «ЦифровойНаряд» (структура,
  роли/роутинг, ручной деплой, верификация `node --check`, Do/Don't, DoD).
- Создан `opencode.json` — `instructions` ссылаются на контракт и specs;
  запреты: `clasp*`, `git push*`, `rm -rf *`; исключены `**/dist/**`, `**/deploy/**`;
  whitelist скиллов — `digitalapps-deploy`.
- Создан `docs/specs/`:
  - `architecture.md` — стек, 12 листов Google, 12 модулей Apps Script,
    точки входа и события, жизненный цикл end-to-end;
  - `roles.md` — роли, роутинг `doGet`, логика входа, расхождения источников;
  - `data-model.md` — точные структуры колонок листов (выверено по коду),
    статусы жизненного цикла;
  - `adr/deploy-manual.md` — ADR-001: деплой вручную, без clasp.
- `README.md` переписан в карту проекта: ссылки на specs, устранены
  расхождения (упоминание `deploy/`, состав листов/модулей, ОТК).
- `.opencode/skills/digitalapps-deploy/SKILL.md` переписан под факт:
  ручной деплой (копирование тела `.md` в редактор Apps Script →
  «Новая версия»), без clasp/`dist/`. Добавлены `ShiftUI` и doc-only файлы,
  процедура «вечной Загрузки» по `docs/МАСТЕР-ДИАГНОСТИКА.md`.
- Инициализирован `docs/reports/` (этот отчёт).

## Как проверено

- Все спецификации сверены с фактическим содержимым код-модулей
  (`modules/Config.md`, `modules/Auth.md`, `modules/Launches.md`,
  `modules/PlanningAPI.md`, `modules/ShiftUI.md`,
  `modules/Shifts.md`, `modules/NaryadAPI.md`, `modules/PrintQueue.md`,
  `modules/CatalogAPI.md`) — структуры колонок и роутинг взяты из кода,
  а не из памяти.
- Код-модули НЕ изменялись — `node --check` не требуется для этого этапа.
- Проверено отсутствие `deploy/`/`dist/` и несоответствия skill (clasp) — устранено.

## Что осталось

- Деплой не требуется (код-модули не менялись).
- Непроверенная (пользователем) роль `shift` — тестовый сотрудник в
  «Сотрудники», копирование ShiftUI/Auth/Launches, деплой, тест.
- ОТК-интерфейс — не начат.

## Следующий шаг

Проверка ShiftUI пользователем; далее — ОТК-интерфейс по stage-модели
(План → Реализация → Проверка → Отчёт).