# STATUS.md — живое состояние проекта «ЦифровойНаряд»

> Единая всегда-актуальная сводка для быстрого входа в контекст нового диалога.
> Читать при старте каждого диалога (см. AGENTS.md → приоритет чтения).
> Обновлять по завершении каждого этапа (см. AGENTS.md → Definition of Done).

## Последний завершённый этап

- **07.09.2026 — Полный E2E-бизнес-цикл PASS (24/24) на новом деплое англоязычных листов.**
  `tests/e2e/test_bizcycle.py` прогнан на активном `/exec`
  (`AKfycbzkYDDqEmavt3QHECKoxyW5AjHlu4C0FtmwATpFAkYCVkv4TT5gjO1JFRgCsr1rzQsdHA`),
  листы подготовлены вручную (Employees/Transitions/ClosedOrders/колонка `Причина доработки`).
  Сквозной цикл: shift выдача → оператор принимает → переход → ОТК «Ждут ОТК» → проверка
  перехода → закрытие → наряд уходит из очереди → оператор видит «Итоги ОТК» → rework-цикл
  (ОТК возврат с причиной → оператор правит → снова waiting_otk → проверка → закрытие).
  По ходу устранены: отказ `isRole(_ndName,'otk')` из-за несоответствия ФИО в `Employees`
  (тест использует фактические ФИО), strict-mode двух «Проверить» (rework-цикл), гонка
  чтения карточки наряда. Наряды N-260907-131911/132034 закрыты в `ClosedOrders`,
  легаси-листы не тронуты (подтверждено пользователем). Отчёт:
  `docs/reports/2026-09-07-e2e-bizcycle.md`.
- **07.09.2026 — Флоу переведён на англоязычные листы (реализация ADR-003, вариант A).**
  Канон наряда — `WorkOrders` (13 колонок, добавлена `Причина доработки`); переходы —
  англоязычный лист `Transitions`; итоги закрытия — `ClosedOrders`; сотрудники — `Employees`.
  Кириллические листы `Наряды`/`Переходы`/`Закрытые` из флоу выведены (легаси, не меняются).
  Оператор/ОТК читают канон напрямую (`getOtkQueue` теперь из `WorkOrders` — закрывает
  «пустую OTK-очередь»), VBA-функции переведены на новые листы. Изменены:
  `Config.md`, `NaryadAPI.md`, `OtkUI.md`, `Auth.md`; спека `data-model.md` актуализирована.
- **07.09.2026 — Подтверждён фикс «Переходы» на новом деплое** (правка литералов
  колонок `Номер наряда`/`№ перехода` в `NaryadAPI.md`; переход оператора
  пишется на лист и виден в карточке наряда).
- **07.09.2026 — Реализован ADR-005 (вариант C): общий словарь статусов наряда.**
  `Config` — единые `NARYAD_STATUS` + `NARYAD_STATUS_LABELS`; серверные статусные
  литералы (`NaryadAPI`, `OtkUI`, `ShiftUI`, `PlanningAPI`) переведены на словарь,
  `setNaryadStatus` валидирует по словарю; клиентские страницы Operator/Otk
  получают словарь через инжекцию `${JSON.stringify(...)}`. Спека обновлена
  (`data-model.md`). Отчёт: `docs/reports/2026-09-07-adr005c-status-dictionary.md`.
- **07.09.2026 — Приняты архитектурные решения (ADR-003/004/005).**
  `WorkOrders` — канон наряда; манифест деплоя `docs/reports/deploy-manifest.md`
  (ADR-004, вариант A); общий словарь статусов (ADR-005, вариант C, реализация —
  отдельным мини-этапом). Тег `v0.4-architecture-decisions`. Коммит см. ниже.
- **04.09.2026 — E2E-тестирование всех ролей + фикс бага №1.**
  Отчёт: `docs/reports/2026-09-04-e2e-roles.md`. Прогоны: operator **6/6**, otk **4/4**,
  master **4/4**, shift **4/4** (итого 18/18 PASS).

## Открытые баги / TODO

- [ ] VBA-перестройка (отдельным этапом): VBA-функции уже пишут в новые листы,
  но полный переезд VBA — позже.
- [ ] Зачистить тестовые строки E2E в листах (`WorkOrders`/`Transitions`/`ClosedOrders`)
  и тестовую смену оператора при согласии пользователя.
- [ ] Дозаполнить строку манифеста деплоя (`docs/reports/deploy-manifest.md`): номер
  версии Apps Script нового деплоя (URL уже актуализирован в STATUS.md).
- [ ] (опц.) Рефакторинг чтения листов WorkOrders/Planning на чтение по именам колонок (`headers.indexOf`).
- [ ] (опц.) Фиксация отображения `rework` в Dashboard/Planning Master (открытый вопрос ADR-005).

## Закрытые баги

- ✅ **«Пустая OTK-очередь» (рассинхрон `waiting_otk`)** — оператор видел статус
  «Ждёт ОТК», но очередь ОТК не показывала наряд: `getOtkQueue` строил очередь из
  листа `Наряды`, куда shift-поток (WorkOrders) не писал. Устранён переводом
  канона на `WorkOrders` (ADR-003, реализован 07.09).
- ✅ **Баг №1 — переавторизация оператора при F5** (после входа URL оставался `?page=login`).
  Причина: оператор единственный логинился через фрагмент-инъекцию без смены URL.
  Фикс: `modules/Auth.md` — редирект на полную `?page=operator&name=...&id=...`.
  Коммит `5022dc9` (тег `v0.2-e2e`). Подтверждено E2E (F5 не возвращает форму входа).
- ✅ **Баг shift-выдачи** (`loadOperators()` обрывалась без вызова серверной функции) — исправлен раньше, отчёт `docs/reports/2026-09-02-shift-role-testing.md`.

## Следующий шаг

- Согласовать и реализовать полный переезд VBA (отдельный этап) и актуализацию
  печати (PrintQueue); при необходимости — зачистка тестовых данных E2E.
- Дозаполнить манифест деплоя (номер версии Apps Script) и закоммитить этап
  по согласию пользователя.

## Последний деплой / активный URL

- Актуальный URL `/exec` (деплой перестройки на англоязычные листы, подтверждён
  полным E2E-бизнес-циклом):
  `https://script.google.com/macros/s/AKfycbzkYDDqEmavt3QHECKoxyW5AjHlu4C0FtmwATpFAkYCVkv4TT5gjO1JFRgCsr1rzQsdHA/exec`
- Журнал деплоев — `docs/reports/deploy-manifest.md` (ADR-004): строка нового
  деплоя ждёт только номер версии Apps Script.
- Dеплойный скрипт: `docs/МАСТЕР-ДИАГНОСТИКА.md` (ШАГ 2).

## Справочники

- E2E-алгоритм запуска: `docs/testing/e2e-runbook.md`.
- Диагностика «Загрузка…»/пустая страница: `docs/МАСТЕР-ДИАГНОСТИКА.md`.
- Архитектура/роли/данные: `docs/specs/architecture.md`, `docs/specs/roles.md`, `docs/specs/data-model.md`.
