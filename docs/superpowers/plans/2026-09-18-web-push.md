# Web Push (Этап 12) Implementation Plan

> **For agentic workers:** Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Нативные push-уведомления на телефон (PWA + Service Worker + web-push) — усиление внутренних уведомлений (Этап 8) для закрытых вкладок.

**Architecture:** Новые подписки в таблице `PushSubscription` (логин + endpoint + keys). dispatch встроен в `notify.ts` (`notifyOperator`/`notifyRole`), хуки событий не меняются. Клиент: Service Worker `/sw.js` + `initPush()` на страницах ролей; подписка создаётся по кнопке «Разрешить уведомления» в dropdown колокольчика (жест — требование iOS) или автоматически, если разрешение уже granted. VAPID из env.

**Tech Stack:** Node 20/Express, `web-push` (npm), Prisma/PostgreSQL, EJS + vanilla JS, Service Worker, Playwright (probe).

**Spec:** `docs/specs/notifications.md` (раздел «Фаза 2 — Web Push», сейчас реализуется)

## Global Constraints

- Проект — монолит `server/`, источник правды `server/prisma/schema.prisma`; миграции только через `prisma migrate dev`.
- Иконки PWA — авторские, генерируются `scripts/make-icons.ts` (чистый Node, zlib; без новых зависимостей для рантайма). Позже заменяются реальным логотипом.
- VAPID-ключи и `APP_URL` — из `server/.env` (не в git); `.env.example` обновляется.
- Аккаунт/подписка `web-push.sendNotification` не должна блокировать хук: fire-and-forget с `.catch`, на статус 404/410 подписка удаляется.
- Подписка привязана к логину (`Employees.login`); один endpoint может быть у многих логинов (upsert по `(login, endpoint)`).
- Клиентская подписка работает только в secure-контексте: localhost (dev), https Funnel (стенд). Безошибочный fallback, если push недоступен.
- Все новые публичные фичи E2E-проганяются: ролевые 26/26 + `test_bizcycle.py` 51/51 НЕ должны ломаться.

---

### Task 1: Модель `PushSubscription` + миграция

**Files:**
- Modify: `server/prisma/schema.prisma`
- Create: миграция (генерируется Prisma, `server/prisma/migrations/<ts>_add_push_subscriptions/`)

**Interfaces:**
- Produces: Prisma model `PushSubscription` с полями `id String @id @default(cuid())`, `login String`, `endpoint String @unique`, `p256dh String`, `auth String`, `createdAt DateTime @default(now())`, `@@index([login])`.

- [ ] **Шаг 1: добавить модель в конец `schema.prisma`**

```prisma
model PushSubscription {
  id        String   @id @default(cuid())
  login     String
  endpoint  String   @unique
  p256dh    String
  auth      String
  createdAt DateTime @default(now())

  @@index([login])
}
```

- [ ] **Шаг 2: сгенерировать миграцию и клиент**

Run: `npx prisma migrate dev --name add_push_subscriptions` (в `server/`, против dev-контура `MOSD`).
Expected: миграция применена, `@prisma/client` перегенерирован.

- [ ] **Шаг 3: проверить схему в стендовом контуре (docker)**

Run: `npm run build` — тип `PushSubscription` доступен; стендовая миграция применится автоматически на деплое (Task 9).

- [ ] **Шаг 4: Commit**

```bash
git add server/prisma/schema.prisma server/prisma/migrations
git commit -m "feat: модель PushSubscription (подписки Web Push по логину) (этап 12)"
```

### Task 2: Зависимость `web-push`, конфиг VAPID/APP_URL, скрипт ключей

**Files:**
- Modify: `server/package.json`, `server/src/config.ts`, `server/.env.example`, `server/.env` (не коммитить), `server/.gitignore` (в silence — `.env` уже исключён)

**Interfaces:**
- Consumes: — 
- Produces: `config.vapidPublicKey: string`, `config.vapidPrivateKey: string`, `config.vapidSubject: string`, `config.appUrl: string`.
- Prooduces: npm-скрипт `"push:keys": "web-push generate-vapid-keys"`.

- [ ] **Шаг 1: установить зависимость**

Run: `npm install web-push` (в `server/`).
Expected: в `package.json` появляется `"web-push": "^3.x"`; транзитивно `@types/web-push` не нужен (пакет содержит типы? — нет, добавить devDep `@types/web-push` под серьёзность: попробовать `npm i -D @types/web-push`; если пакета нет — объявить типы в `server/src/types/web-push.d.ts` вручную в Task 3).

- [ ] **Шаг 2: добавить npm-скрипт в `package.json`**

```json
"push:keys": "web-push generate-vapid-keys"
```

- [ ] **Шаг 3: расширить `config.ts`**

```ts
export const config = {
  port: parseInt(process.env.PORT || '3000', 10),
  jwtSecret: process.env.JWT_SECRET || 'dev-secret',
  vbaSecret: process.env.VBA_SECRET || 'dev-vba-secret',
  databaseUrl: process.env.DATABASE_URL || 'postgresql://localhost:5432/digital_narad',
  appUrl: process.env.APP_URL || `http://localhost:${process.env.PORT || 3000}`,
  vapidPublicKey: process.env.VAPID_PUBLIC_KEY || '',
  vapidPrivateKey: process.env.VAPID_PRIVATE_KEY || '',
  vapidSubject: process.env.VAPID_SUBJECT || '',
};
```

- [ ] **Шаг 4: `.env.example`** — добавить блок Web Push с инструкцией генерации:

```ini
# === Web Push (PWA) ===
# Генерация VAPID: npm run push:keys
# APP_URL — публичный https-адрес стенда (для ссылок в push-уведомлениях)
APP_URL=http://localhost:3000
VAPID_PUBLIC_KEY=
VAPID_PRIVATE_KEY=
VAPID_SUBJECT=mailto:admin@example.com
```

- [ ] **Шаг 5: сгенерировать dev-ключи и записать в `server/.env`** (не коммитить)

Run: `npx web-push generate-vapid-keys --json` (в `server/`) → подставить публичный/приватный ключ в `.env`. `VAPID_SUBJECT=mailto:admin@example.com`.

- [ ] **Шаг 6: Commit**

```bash
git add server/package.json server/package-lock.json server/src/config.ts server/.env.example
git commit -m "chore: web-push зависимость, VAPID/APP_URL конфиг и скрипт ключей (этап 12)"
```

### Task 3: `lib/push.ts` — диспетчер отправки + интеграция в `notify.ts`

**Files:**
- Create: `server/src/lib/push.ts`
- Modify: `server/src/lib/notify.ts`

**Interfaces:**
- Consumes: `prisma` (из `../lib/prisma`), `config`, `NotificationData` из `notify.ts`.
- Produces:
  - `export function isPushConfigured(): boolean` — true, если заданы оба VAPID-ключа.
  - `export async function dispatchPush(logins: string[], data: NotificationData): Promise<void>` — выбирает подписки по `login in logins`, отправляет каждую, на 404/410 удаляет; все ошибки проглатываются.
- Помечено структурное: `NotificationData` (`{ type, title, message?, link? }`) переносится из `notify.ts` в общий модуль? Нет — `push.ts` определяет собственный параметр `{ title, message?, link? }` (универсальный месседж-объект), `notify.ts` передаёт свои `data`. Импорт типа из `notify.ts` в `push.ts` — циклическая зависимость → **использовать `import type { NotificationData } from './notify'`** (только тип, эрейзится).

- [ ] **Шаг 1: написать `lib/push.ts`**

```ts
import webpush from 'web-push';
import { config } from '../config';
import { prisma } from './prisma';
import type { NotificationData } from './notify';

export function isPushConfigured(): boolean {
  return Boolean(config.vapidPublicKey && config.vapidPrivateKey);
}

export async function dispatchPush(logins: string[], data: NotificationData): Promise<void> {
  if (!isPushConfigured() || !logins.length) return;
  try {
    const subs = await prisma.pushSubscription.findMany({ where: { login: { in: logins } } });
    if (!subs.length) return;
    const payload = JSON.stringify({
      title: data.title,
      message: data.message || '',
      link: data.link || '/',
      url: config.appUrl + (data.link || '/'),
      type: data.type,
    });
    await Promise.all(subs.map(async (s) => {
      try {
        await webpush.sendNotification(
          { endpoint: s.endpoint, keys: { p256dh: s.p256dh, auth: s.auth } },
          payload,
          { ttl: 86400 },
        );
      } catch (err: unknown) {
        const code = (err as { statusCode?: number }).statusCode;
        if (code === 404 || code === 410) {
          await prisma.pushSubscription.delete({ where: { endpoint: s.endpoint } }).catch(() => {});
        } else {
          console.error('[push] send failed:', code ?? err);
        }
      }
    }));
  } catch (err) {
    console.error('[push] dispatch error:', err);
  }
}
```

- [ ] **Шаг 2: типы для VAPID** — если `npm i -D @types/web-push` недоступен, проверить встроенные типы пакета; при отсутствии — максимально простой фаил деклараций `server/src/types/web-push.d.ts`:

```ts
declare module 'web-push' {
  interface PushSubscriptionKeys { p256dh: string; auth: string; }
  interface PushSubscription { endpoint: string; keys: PushSubscriptionKeys; }
  interface SendOptions { ttl?: number; }
  const webpush: {
    setVapidDetails(subject: string, publicKey: string, privateKey: string): void;
    sendNotification(subscription: PushSubscription, payload?: string, options?: SendOptions): Promise<void>;
  };
  export default webpush;
}
```

- [ ] **Шаг 3: инициализация + интеграция в `notify.ts`**

```ts
import webpush from 'web-push';
import { config } from '../config';
import { dispatchPush, isPushConfigured } from './push';

if (isPushConfigured()) {
  webpush.setVapidDetails(
    config.vapidSubject || config.appUrl,
    config.vapidPublicKey,
    config.vapidPrivateKey,
  );
}
```

`notifyOperator` (после create):

```ts
if (emp) {
  await prisma.notification.create({ data: { ...data, targetLogin: emp.login } });
  void dispatchPush([emp.login], data);
}
```

`notifyRole` — резолвит логины роли и шлёт:

```ts
export async function notifyRole(role: EmployeeRole, data: NotificationData) {
  const emp = await prisma.employees.findMany({ where: { role }, select: { login: true } });
  await prisma.notification.create({ data: { ...data, targetRole: role } });
  void dispatchPush(emp.map((e) => e.login), data);
}
```

- [ ] **Шаг 4: проверка**

Run: `npm run build` + `npm run check` в `server/`.
Expected: PASS.

- [ ] **Шаг 5: Commit**

```bash
git add server/src/lib/push.ts server/src/lib/notify.ts server/src/types server/package.json server/package-lock.json
git commit -m "feat: Web Push диспетчер (lib/push.ts) и интеграция в notifyOperator/notifyRole (этап 12)"
```

### Task 4: `routes/push.routes.ts` — VAPID-ключ, subscribe, unsubscribe

**Files:**
- Create: `server/src/routes/push.routes.ts`
- Modify: `server/src/app.ts`

**Interfaces:**
- Consumes: `requireAuth` (middeware), `prisma`, `config`, `lib/request.param`.
- Produces: default-роутер:
  - `GET /vapid-key` (requireAuth) → `{ publicKey }` (пустая строка, если не настроен)
  - `POST /subscribe` (requireAuth) → body `{ endpoint, keys: { p256dh, auth } }`; валидация; upsert `(login, endpoint)`; → `{ ok: true }`
  - `POST /unsubscribe` (requireAuth) → body `{ endpoint }`; deleteMany `(login, endpoint)`; → `{ ok: true }`

- [ ] **Шаг 1: написать роутер**

```ts
import { Router } from 'express';
import { prisma } from '../lib/prisma';
import { requireAuth, AuthRequest } from '../middleware/auth';
import { config } from '../config';

const router = Router();

function validateEndpoint(endpoint: string): boolean {
  return typeof endpoint === 'string' && /^https?:\/\//.test(endpoint) &&
    (/^https:\/\//.test(endpoint) || endpoint.startsWith('http://localhost'));
}

router.get('/vapid-key', requireAuth('operator', 'shift', 'otk', 'master'), (_req, res) => {
  res.json({ publicKey: config.vapidPublicKey || '' });
});

router.post('/subscribe', requireAuth('operator', 'shift', 'otk', 'master'), async (req: AuthRequest, res) => {
  const user = req.user!;
  const { endpoint, keys } = req.body || {};
  if (!validateEndpoint(endpoint) || !keys || !keys.p256dh || !keys.auth) {
    res.status(400).json({ error: 'Невалидная push-подписка' });
    return;
  }
  await prisma.pushSubscription.upsert({
    where: { endpoint },
    create: { login: user.login, endpoint, p256dh: keys.p256dh, auth: keys.auth },
    update: { login: user.login, p256dh: keys.p256dh, auth: keys.auth },
  });
  res.json({ ok: true });
});

router.post('/unsubscribe', requireAuth('operator', 'shift', 'otk', 'master'), async (req: AuthRequest, res) => {
  const user = req.user!;
  const { endpoint } = req.body || {};
  if (endpoint) {
    await prisma.pushSubscription.deleteMany({ where: { login: user.login, endpoint } });
  }
  res.json({ ok: true });
});

export default router;
```

- [ ] **Шаг 2: подключить в `app.ts`**

```ts
import pushRoutes from './routes/push.routes';
// …
app.use('/api/push', wrapRouter(pushRoutes));
```

- [ ] **Шаг 3: проверка** — `npm run build` + `npm run check` PASS.

- [ ] **Шаг 4: Commit**

```bash
git add server/src/routes/push.routes.ts server/src/app.ts
git commit -m "feat: REST /api/push — vapid-key/subscribe/unsubscribe (этап 12)"
```

### Task 5: Service Worker `public/sw.js`

**Files:**
- Create: `server/src/public/sw.js`

**Interfaces:**
- Produces: SW в scope `/`: обрабатывает `push` (показывает уведомление `{title, body, icon: '/icons/icon-192.png', badge: '/icons/icon-192.png', data: {url}}`) и `notificationclick` (close, focus/открыть url).

- [ ] **Шаг 1: написать SW**

```js
self.addEventListener('push', function (event) {
  var data = {};
  try { data = event.data ? event.data.json() : {}; } catch (e) {}
  var title = data.title || 'ЦифровойНаряд';
  var options = {
    body: data.message || '',
    icon: '/icons/icon-192.png',
    badge: '/icons/icon-192.png',
    data: { url: data.url || '/' },
    ...
  };
  event.waitUntil(self.registration.showNotification(title, options));
});

self.addEventListener('notificationclick', function (event) {
  event.notification.close ();
  var url = (event.notification.data && event.notification.data.url) || '/';
  event.waitUntil(clients.matchAll({ type: 'window', includeUncontrolled: true }).then(function (list) {
    for (var i = 0; i < list.length; i++) {
      var client = list[i];
      if (client.url.indexOf(url) !== -1 && 'focus' in client) return client.focus();
    }
    return clients.openWindow(url);
  }));
});
```

- [ ] **Шаг 2: Commit**

```bash
git add server/src/public/sw.js
git commit -m "feat: Service Worker push/notificationclick (этап 12)"
```

### Task 6: `public/push-init.js` + кнопка в колокольчике (`ui.js`)

**Files:**
- Create: `server/src/public/push-init.js`
- Modify: `server/src/public/ui.js`

**Interfaces:**
- Consumes: `api()` (api.js), `showToast()`.
- Produces:
  - `async function initPush()` — регистрирует SW, подписывается если permission granted; регистрирует обработчик для кнопки.
  - window-функция `askPushPermission()` — запрашивает permission, подписывает; вызывается из кнопки.

**Логика авто-подписки:** если `Notification.permission === 'granted'` и SW зарегистрирован → проверить `pushManager.getSubscription()`; если есть → `POST /subscribe` (upsert на сервере) и сохранить `localStorage.pushEndpoint`; если нет и есть VAPID-ключ → subscribe.

**UI:** в `initNotifications()` добавить кнопку в шапку dropdown: «Разрешить уведомления» → `askPushPermission()`. Кнопка показывается только если push доступен (SW + https/localhost) и permission ещё не granted.

- [ ] **Шаг 1: `push-init.js`**

```js
function urlBase64ToUint8Array(base64String) {
  var padding = '='.repeat((4 - (base64String.length % 4)) % 4);
  var base64 = (base64String + padding).replace(/-/g, '+').replace(/_/g, '/');
  var raw = atob(base64);
  var bytes = new Uint8Array(raw.length);
  for (var i = 0; i < raw.length; i++) bytes[i] = raw.charCodeAt(i);
  return bytes;
}

function pushSupported() {
  return ('serviceWorker' in navigator) && ('PushManager' in window);
}

var cachedVapidKey = null;
async function getPushConfig() {
  if (!pushSupported()) return null;
  try {
    if (!cachedVapidKey) {
      var data = await api('/api/push/vapid-key');
      cachedVapidKey = data.publicKey || '';
    }
    return cachedVapidKey || null;
  } catch (e) { return null; }
}

async function ensureServiceWorker() {
  try {
    return await navigator.serviceWorker.register('/sw.js');
  } catch (e) { return null; }
}

async function attachPushSubscription(registration) {
  var key = await getPushConfig();
  if (!key || !registration || !registration.pushManager) return;
  try {
    var subscription = await registration.pushManager.getSubscription();
    if (!subscription) {
      subscription = await registration.pushManager.subscribe({
        userVisibleOnly: true,
        applicationServerKey: urlBase64ToUint8Array(key)
      });
    }
    var ep = subscription.endpoint;
    try {
      await api('/api/push/subscribe', {
        method: 'POST',
        body: {
          endpoint: ep,
          keys: {
            p256dh: btoa(String.fromCharCode.apply(null, new Uint8Array(subscription.getKey('p256dh')))),
            auth: btoa(String.fromCharCode.apply(null, new Uint8Array(subscription.getKey('auth'))))
          }
        }
      });
      try { localStorage.setItem('pushEndpoint', ep); } catch (e2) {}
      return true;
    } catch (eNotify) {
      await subscription.unsubscribe();
    }
  } catch (e) {
    console.warn('[push] attach failed', e);
  }
  return false;
}

async function initPush() {
  if (!pushSupported()) return;
  var registration = await ensureServiceWorker();
  if (!registration) return;
  if (Notification.permission === 'granted') {
    await attachPushSubscription(registration);
  }
}

window.askPushPermission = async function () {
  if (!pushSupported()) { showToast('Push недоступен в этом браузере', true); return; }
  var permission = await Notification.requestPermission();
  if (permission !== 'granted') { showToast('Уведомления отключены', true); return; }
  var registration = await ensureServiceWorker();
  var ok = await attachPushSubscription(registration);
  showToast(ok ? 'Push-уведомления включены' : 'Не удалось включить push', !ok);
  reloadNotifPanel && reloadNotifPanel();
};
```

- [ ] **Шаг 2: кнопка в `ui.js`**

В `initNotifications()`: после создания `bell.firstChild` (кнопка-колокольчик) дополнить панель флагом: завести `window.reloadNotifPanel = function(){ poll(); }`. В `renderNotifPanel`/`open` вставить блок в шапку dropdown:

```js
function notifPushHeader() {
  if (!('serviceWorker' in navigator) || !('PushManager' in window)) return '';
  if (typeof Notification !== 'undefined' && Notification.permission === 'granted') return '';
  return '<div class="notif__push"><button type="button" class="btn btn--sm" onclick="askPushPermission()">Разрешить уведомления</button></div>';
}
```

Вставить результат `notifPushHeader()` в начало `panel.innerHTML` при рендере (в `renderNotifPanel`) и в `open()`.

- [ ] **Шаг 3: Commit**

```bash
git add server/src/public/push-init.js server/src/public/ui.js
git commit -m "feat: клиент push (initPush + кнопка в колокольчике) (этап 12)"
```

### Task 7: PWA-манифест, meta-тэги в EJS, генерация иконок

**Files:**
- Create: `server/src/public/manifest.webmanifest`
- Create: `server/src/scripts/make-icons.ts` → PNG в `server/src/public/icons/` (192/512/180) + SVG-превью
- Modify: `server/src/views/login.ejs`, `server/src/views/operator.ejs`, `server/src/views/master-app.ejs`, `server/src/views/shift-app.ejs`, `server/src/views/otk-app.ejs` (head)
- Modify: `server/src/views/operator.ejs`, `master-app.ejs`, `shift-app.ejs`, `otk-app.ejs` (подключение `push-init.js` + вызов `initPush()`)

**Interfaces:**
- Produces: `manifest.webmanifest` (name «ЦифровойНаряд», `display: standalone`, `start_url: /`, icons 192/512, theme_color `#131c30`, background `#131c30`); `apple-touch-icon` 180.

- [ ] **Шаг 1: `manifest.webmanifest`**

```json
{
  "name": "ЦифровойНаряд",
  "short_name": "Наряд",
  "description": "Производство подъёмных агрегатов — цифровые наряды",
  "start_url": "/",
  "display": "standalone",
  "background_color": "#131c30",
  "theme_color": "#131c30",
  "icons": [
    { "src": "/icons/icon-192.png", "sizes": "192x192", "type": "image/png", "purpose": "any" },
    { "src": "/icons/icon-512.png", "sizes": "512x512", "type": "image/png", "purpose": "any maskable" }
  ]
}
```

- [ ] **Шаг 2: `scripts/make-icons.ts`** — генерит square PNG (RGBA) через zlib (без canvas): тёмно-синий фон `#131c30`, в центре силуэт-стрела (подъёмный агрегат) + белый квадратик-бейдж «ЦН» (две буквы рисуются прямоугольными сегментами 7-сегментнымим шрифтом — упрощённо: «Ц» и «Н» как rect-группы). Энкодер:

```ts
import { deflateSync } from 'zlib';
import { mkdirSync, writeFileSync } from 'fs';
import path from 'path';

function crc32(buf: Buffer): number { /* таблица CRC32 */ }
function chunk(type: string, data: Buffer): Buffer { /* длина+тип+данные+CRC */ }
function encodePng(w: number, h: number, rgba: Buffer): Buffer { /* IHDR+IDAT(deflate)+IEND */ }
function rect(img: Buffer, w: number, x: number, y: number, rw: number, rh: number, color: [number,number,number,number]): void { /* заливка прямоугольника */ }
const COLORS = { bg: [19,28,48,255], fg: [167,213,255,255], accent: [37,99,235,255], white: [255,255,255,255] };
```

Рисунок (canvas `size`, масштаб `u = size/512`): фон bg; трапеция/стрела accent c контуром fg, платформа; буквы «Ц» и «Н» белые с fg-обводкой. Скрипт создаёт 512, 192, 180 из 512-картинки (бикубический вам не нужен — простая nearest/clustered для placeholder).

- [ ] **Шаг 3: запустить генерацию иконок**

Run: `npx tsx server/scripts/make-icons.ts` (в `server/`).
Expected: `server/src/public/icons/{icon-192,icon-512,apple-touch-icon-180}.png` созданы (файлы меньше ~20КБ).

- [ ] **Шаг 4: `<head>` всех 5 вью**

```html
<link rel="manifest" href="/manifest.webmanifest">
<meta name="theme-color" content="#131c30">
<link rel="icon" href="/icons/icon-192.png" type="image/png">
<link rel="apple-touch-icon" href="/icons/apple-touch-icon-180.png">
<meta name="apple-mobile-web-app-capable" content="yes">
<meta name="apple-mobile-web-app-status-bar-style" content="black-translucent">
<meta name="apple-mobile-web-app-title" content="Наряд">
```

- [ ] **Шаг 5: `push-init.js` на страницах ролей**

После `<script src="/ui.js"></script>` добавить `<script src="/push-init.js"></script>`, и в основном inline-скрипте (внутри проверки роли) `initPush();` рядом с `initNotifications();`.

- [ ] **Шаг 6: проверка** — `npm run build` включает копирование `src/public` → `dist/public`? (см. Dockerfile: `COPY src/public ./dist/public`) да. Локально `npm run dev` отдаёт из `src/public`.

- [ ] **Шаг 7: Commit**

```bash
git add server/src/public/manifest.webmanifest server/src/public/icons server/scripts/make-icons.ts server/src/views
git commit -m "feat: PWA manifest, meta-тэги, push-init и авторские иконки (этап 12)"
```

### Task 8: Проверки сборки + push-проб PWA (Playwright probe)

**Files:**
- Create: probe (временный в `C:\Users\73B5~1\AppData\Local\Temp\opencode\probe_push.py`; не коммитится)

**Interfaces:**
- Consumes: работающий сервер (localhost:3000), JWT-логин через `/login`.

- [ ] **Шаг 1: `npm run build` + `npm run check`** в `server/` — PASS.

- [ ] **Шаг 2: Проб-скрипт Playwright (headless, `context.grant_permissions=['notifications']`)**

```python
# probe_push.py — сводка:
# 1) GET /api/push/vapid-key без токена -> 401
# 2) login operator -> токен; GET /api/push/vapid-key -> non-empty publicKey
# 3) navigator.serviceWorker.register('/sw.js') + navigator.serviceWorker.ready
# 4) pushManager.subscribe(userVisibleOnly, applicationServerKey) -> endpoint
# 5) POST /api/push/subscribe -> ok:true
# 6) БД: SELECT count(*) FROM "PushSubscription" WHERE login=operator AND endpoint=... -> 1
# 7) POST /api/push/unsubscribe -> ok:true; БД count -> 0
```

Подробный вывод каждого шага; assert по результату.

- [ ] **Шаг 3: прогон против локального dev-сервера или Docker-стенад** — все шаги PASS.

### Task 9: Deploy — пересборка образа, миграция на стенде

- [ ] **Шаг 1**: в `server/` — `docker compose up -d --build` (по явному согласию пользователя); `prisma migrate deploy` выполнит Migrate `add_push_subscriptions` в контейнере.
- [ ] **Шаг 2**: `GET /health` → `{status:'ok', db:'connected'}`.
- [ ] **Шаг 3**: сообщить пользователю «тестируй» (push на телефоне: iOS «На главный экран» / Android).

### Task 10: E2E-регресс + ручной push на телефоне

- [ ] Ролевые: `test_operator/otk/master/shift.py` — 26/26 PASS.
- [ ] Бизнес-цикл: `test_bizcycle.py` — 51/51 PASS.
- [ ] Ручной: закрыть вкладку оператора, выдать наряд из shift → на телефоне виден native push.

### Task 11: Документация + финальный коммит/тег

- [ ] `docs/specs/notifications.md` — раздел «Фаза 2 Web Push» → «реализовано» (модель, роут, клиент, ограничения iOS, проверки).
- [ ] `docs/specs/architecture.md` — модель PushSubscription, роут `/api/push`, `lib/push.ts`, конвенция VAPID/APP_URL.
- [ ] `docs/reports/2026-09-18-web-push.md` — отчёт этапа.
- [ ] `docs/reports/STATUS.md` — Этап 12, открытые баги, следующий шаг.
- [ ] Commit docs + tag `v0.15-web-push`.